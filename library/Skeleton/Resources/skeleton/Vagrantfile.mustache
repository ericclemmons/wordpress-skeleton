Vagrant::Config.run do |config|
  config.vm.box       = "lucid32"
  config.vm.box_url   = "http://files.vagrantup.com/lucid32.box"
  config.vm.host_name = "{{deploy.develop.web.name}}"

  # Dedicated IP to avoid conflicts (and no port fowarding!)
  config.vm.network :hostonly, "{{deploy.develop.web.host}}"

  # Mount the src folder as NFS for caching & speed
  config.vm.share_folder("v-root", "/vagrant", ".", :nfs => true)

  config.vm.provision :chef_solo do |chef|
    chef.cookbooks_path = ["cookbooks", "vendor/cookbooks"]

    chef.add_recipe("wordpress-skeleton");
  end
end
